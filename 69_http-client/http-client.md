Requires network access. On Windows, link with -lws2_32.

___

##### Run Command:
`zig run http-client.zig`

##### Results:
`Response status: 200 OK`
`<!DOCTYPE html>`
`<html>`
`  <head>`
`    <meta charset="utf-8">`
