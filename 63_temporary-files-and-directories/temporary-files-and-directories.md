Temp file and directory names vary with each run; Zig has no stdlib TempFile function so names are generated using a timestamp.

___

##### Run Command:
`zig run temporary-files-and-directories.zig`

##### Results:
`Temp file name: .../sample...`
`Temp dir name: .../sampledir...`
