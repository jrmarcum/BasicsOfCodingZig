Starts a TCP server on port 7777; connect with a TCP client to send a line and receive an uppercased ACK response. Press Ctrl-C to stop. On Windows, link with -lws2_32.

___

##### Run Command:
`zig run tcp-server.zig`

##### Results:
`TCP server listening on port 7777`
