Zig has no context package; cancellation is implemented using a shared atomic flag. Starts a server on port 8090; press Ctrl-C to stop.

___

##### Run Command:
`zig run context.zig`

##### Results:
`Listening on http://localhost:8090/hello`
`server: hello handler started`
`server: hello handler ended`
