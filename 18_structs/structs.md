Zig structs are printed manually; the format matches Go's `{Name Age}` style.

___

##### Run Command:
`zig run structs.zig`

##### Results:
`{Bob 20}`
`{Alice 30}`
`{Fred 0}`
`&{Ann 40}`
`&{Jon 42}`
`Sean`
`50`
`51`
